package Entity.Inanimates;

import Map.Coordinates;
import Map.Map;

public class Rock extends Inanimate{
    public Rock(Coordinates coordinates, String sprite, Map map) {
        super(coordinates, sprite, map);
    }
}
