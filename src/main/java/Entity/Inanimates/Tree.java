package Entity.Inanimates;

import Map.Coordinates;
import Map.Map;

public class Tree extends Inanimate{
    public Tree(Coordinates coordinates, String sprite, Map map) {
        super(coordinates, sprite, map);
    }
}
